import react from 'react';

interface SchemaFormProps {
    className?: string;
}

const SchemaForm: React.FC<SchemaFormProps> = () => {
	return (
		<>
			<div>is me, open the door</div>
		</>
	);
};
export default SchemaForm;